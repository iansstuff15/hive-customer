import 'dart:io';

class Customer {
  String? firstName;
  String? lastName;
  String? email;
  String? phone;
  String? imageUrl;
  Customer(
      {this.firstName, this.lastName, this.email, this.phone, this.imageUrl});
}
